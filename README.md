Example framework showing use of:
* Cucumber-JVM
* Selenium
* Feign (REST client)
* swagger-codegen tool
* WireMock (+ FreeMarker template)


### swagger-codegen
Generate HTTP client with swagger-codegen tool by running:
```
mvn clean compile
```